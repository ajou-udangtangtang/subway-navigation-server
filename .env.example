# Copy this file to .env and adjust values for your local environment.
# .env is gitignored.

# Database (Team B's territory — leave defaults for now)
DB_HOST=localhost
DB_PORT=3306
DB_USER=subway_user
DB_PASSWORD=
DB_NAME=subway

# KNN
KNN_K=5

# Flask
FLASK_DEBUG=1
