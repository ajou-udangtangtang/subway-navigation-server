# 2026-06-03 — API 로그에 한국어 위치 라벨 추가 (현장 디버깅용)

## 한 일
- `access_log.py`: `/locate` 응답에 `node_kr`, `/route` 응답에 `path_kr` 필드 추가 (로그에만, API 응답 계약은 불변)
- startup 시 `data/nodes.json`의 한국어 `description` 1회 로드 → 노드 ID 매핑 (파일 없으면 빈 매핑 폴백)
- 라이브 서버 재시작(:5001) + ngrok 공개주소 정상 확인, 테스트 66 그린

## 왜 이렇게 했는지
- 현장 통합 테스트 중 로그가 `down_platform` 같은 영문 ID라 읽기 어려움 → 한국어로 즉시 식별
- 응답 본문이 아니라 로그 record에만 추가 → 앱↔서버 계약 안 건드림
- 별도 뷰어 스크립트 대신 로그 자체에 박는 방식 선택 (서버 잠깐 꺼도 된다고 확인받음)

## 막힌 것 / 결정 미뤄진 것
- **핵심 의심**: 현장에서 "입구인데 개찰구로 뜸" 증상인데, 서버 로그엔 폰의 진짜 `/locate`(AP 20개)가 한 건도 없음 (전부 AP 1개짜리 curl 테스트). 앱 자체 더미 fallback 가능성 → 테스트 재개 시 tail로 폰 호출이 실제로 찍히는지부터 확인 필요
- 데이터 추가 수집은 안 하기로 결정됨 (KNN/데이터 손 안 댐)

## 다음에 할 일
- 테스트 재개 시 `tail -f logs/api.jsonl` 로 폰 `/locate` 도달 여부 확인 → 안 찍히면 앱 서버주소/fallback 점검 (앱팀)
- 찍히면 `req.wifi` vs `node_kr` 로 입구↔개찰구 fingerprint 혼동 정량화

## 관련 커밋
- (이 커밋) feat: 한국어 위치 라벨(node_kr/path_kr) API 로그에 추가
